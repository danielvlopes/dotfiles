#!/usr/bin/env ruby

require "fileutils"

original    = ARGV[0]
destination = ARGV[1]

if original.nil? || destination.nil? 
 puts "You should provide an original and destination path" 
else
 FileUtils.mkdir_p destination
 `rsync -a --delete-excluded #{original} #{destination}`
end